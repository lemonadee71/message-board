{% extends "layouts/base.njk" %}
{% from "components/buttons.njk" import get_btn, post_btn %}

{% block body %}
  {# Header #}
  <header class="flex flex-row justify-between content-center px-8 pt-6 pb-4 mb-8">
    <h1 class="font-bold text-xl align-middle">
      <a href="/">Privvit</a>
    </h1>
    <div class="space-x-3">
      {% if current_user %}
        {{ post_btn('Logout', '/logout') }}
      {% else %}
        {{ get_btn('Sign Up', '/signup', class="btn--primary-r") }}
        {{ get_btn('Login', '/login') }}
      {% endif %}
    </div>
  </header>
  <section>
    {% if messages %}
      {% include "components/alert.njk" %}
    {% endif %}
  </section>
  <main class="container-auto">
    {% block main %}{% endblock %}
  </main>
{% endblock %}